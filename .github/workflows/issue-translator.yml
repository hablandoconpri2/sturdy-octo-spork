name: issue-translator

on: 
  issues:
    types: [opened]
  issue_comment: 
    types: [created]  
  
jobs:
  concho: 
    runs-on: ubuntu-latest
    steps:
      - uses: tomsun28/issues-translate-action@v2.6
